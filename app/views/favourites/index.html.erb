<div class="btn-back">
  <%= link_to "< Back to map", vans_path %>
</div>

<div class="container-form">
  <div class="my-form">
    <h2><%= current_user.first_name %>'s favourite Vans</h2>
    <div class="card">
      <div class="card-header">
        <p>Van</p>
      </div>
      <% if @favourites.any? %>
        <% @favourites.each do |favourite| %>
          <div class="card-details">
            <p><%= link_to favourite.van.name, van_path(favourite.van) %></p>
            <p><%= button_to "Remove", favourite_path(favourite), method: :delete, data: {turbo: false}, class: "btn-main" %></p>
          </div>
        <% end %>
      <% else %>
        <p>No vans in your favourites list.</p>
      <% end %>
    </div>
  </div>
</div>
